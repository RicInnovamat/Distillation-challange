You are solving equational implication problems over magmas (set + one binary operation *, no other axioms).
Task: does {{ equation1 }} imply {{ equation2 }} over ALL magmas?

OUTPUT FORMAT (mandatory)
- Plain text only.
- No extra headings or notes.
- `REASONING:` must always be filled.
- `VERDICT:` must be exactly `TRUE` or `FALSE`.
- If `VERDICT: TRUE`, `PROOF:` must be non-empty.
- If `VERDICT: FALSE`, `PROOF:` must be empty and `REASONING:` must name the specific FALSE RULE (e.g. "spine isolation", "word exclusion") or give an explicit COUNTEREXAMPLE with the magma table and a failing assignment.
HARD-STOP: Once ANY rule fires a verdict (TRUE or FALSE), emit VERDICT immediately and STOP. Never continue reasoning. Never override.

GLOBAL DISCIPLINE
- This is a deterministic classifier, not free-form proving.
- Follow STEPS in order. Stop at the first decisive route.
- Never override an exact route with free-form algebra.

STEP 1. EXTRACT FEATURES
Read Eq1 and Eq2 as binary trees. Definitions:
- leftmost/rightmost variable = first/last leaf in left-to-right reading
- WORD(term) = full leaf list
- PARITY(term) = odd/even count per variable
- SET(term) = distinct variables
- bare = one side is a single variable

STEP 2. TRUE ROUTES
2.1 Exact identity
If Eq2 is identical to Eq1 up to consistent variable renaming, `VERDICT: TRUE`.
RULE: exact renaming

2.2 Tautological target
If Eq2 can be renamed x = x, `VERDICT: TRUE`.
RULE: tautological target

2.3 Singleton / collapse source
If Eq1 can be renamed x = T AND x does not appear in T, then Eq1 forces a singleton magma. `VERDICT: TRUE`. Side-swapped T = x also counts.
Example: Eq1 = x = y*z → x not in y*z → singleton → TRUE for any Eq2.
RULE: singleton collapse

2.4 Constant-operation source
If Eq1 can be renamed x*y = T where T is a product (not single variables) AND vars(T) ∩ {x,y} = ∅,
THEN product is constant: x*y = k. Substitute in Eq2. If both sides reduce to the same variable `VERDICT: TRUE`; else `VERDICT: FALSE`.
Example: Eq1: x*y = u​*(v​*w​) →  vars(T)={u,v,w} and Eq2: x*x = x*(y*y), then `VERDICT: TRUE`.
RULE: constant operation

2.5 Source-collapse lemmas
If Eq 1 is bare and can be renamed x = T, match Eq1 against:
- x = x*(y*(z*(x*y))) → LEFT PROJ (p*q=p)
- x = x*((y*z)*(z*z)) → LEFT PROJ (p*q=p)
- x = (((y*z)*x)*z)*x → RIGHT PROJ (p*q=q)
If match, apply the induced projection to Eq2. If both sides reduce to the same variable `VERDICT: TRUE`; else `VERDICT: FALSE`.
RULE: source collapse

2.6 Left projection 
If Eq1 is bare and can be renamed x = x * T,
MANDATORY CHECK: List all variables in T. Does T contain x? YES → SKIP this rule. NO → continue.
Example: x = x * ((x*x)*y) → T = (x*x)*y → vars(T) = {x,y} → T contains x → SKIP.
Example: x = x * (y*z) → T = y*z → vars(T) = {y,z} → T does NOT contain x → APPLY.
If T does NOT contain x → forces a*b=a.
If Eq2 holds applying LEFT PROJ (p*q=p) recursively, then `VERDICT: TRUE`; else `VERDICT: FALSE`.
RULE: left projection

2.7 Right projection
If Eq 1 is bare and can be renamed x = T * x,
MANDATORY CHECK: List all variables in T. Does T contain x? YES → SKIP this rule. NO → continue.
Example: x = (y*x) * x → T = y*x → vars(T) = {y,x} → T contains x → SKIP.
Example: x = (y*z) * x → T = y*z → vars(T) = {y,z} → T does NOT contain x → APPLY.
If T does NOT contain x → forces a*b=b.
If Eq2 holds applying RIGHT PROJ (p*q=q) recursively,  then `VERDICT: TRUE`; else `VERDICT: FALSE`.
RULE: right projection

2.8 Bare-source contradiction motifs
If Eq1 can be renamed x = P, compute features of P:
- rv = number of distinct variables of P,
- Lx = is x the leftmost variable of P?
- Rx = is x the rightmost variable of P?
- xTop = at the top product split U*V of P, does x occur only in U (left), only in V (right), or both?
- ts= v-m if top is variable*product, m-v if product*variable, m-m if product*product
- rTot = sorted occurrence counts of all distinct variables of P
- xCount = occurrences of x in P
MANDATORY: Write all 7 features as a vector:  
Format: rv=_, Lx=_, Rx=_, xTop=_, ts=_, rTot=_, xCount=_
Compare the vector against each motif below. If one motif matches:
C1:  rv >= 4, Lx=FALSE, Rx=FALSE
C2:  rv=3, Rx=FALSE, xTop=right, ts=v-m, xCount=2, rTot=[1,2,2]
C3:  rv=3, xTop=right, ts=v-m, xCount=2, rTot=[1,2,3]
C4:  rv=3, Lx=FALSE, Rx=FALSE, xTop=right, ts=v-m, rTot=[1,1,3]
C5:  rv=3, Lx=FALSE, Rx=FALSE, xTop=left, ts=m-v, rTot=[1,1,3]
C6:  xCount=0
C7:  rv=3, Lx=FALSE, Rx=FALSE, xTop=left, ts=m-m, rTot=[1,2,3], xCount=1
C8:  rv=3, Lx=FALSE, Rx=FALSE, xTop=right, ts=m-m, rTot=[1,2,3], xCount=1
C9:  rv=3, Lx=FALSE, Rx=FALSE, xTop=left, ts=m-v, rTot=[1,2,2], xCount=2
C10: rv=3, Lx=FALSE, Rx=FALSE, xTop=right, ts=v-m, rTot=[1,1,4], xCount=1
C11: rv=3, Lx=FALSE, Rx=FALSE, xTop=right, ts=v-m, rTot=[1,2,3], xCount=3
then `VERDICT: TRUE`
Example: x = (y*(z*x))*z → P = (y*(z*x))*z → rv=3, Lx=FALSE, Rx=FALSE, xTop=left, ts=m-v, rTot=[1,1,3], xCount=1 → matches C5 → `VERDICT: TRUE`.
RULE: contradiction motif

2.9 Extended projection family
If Eq1 can be renamed either
- x = (x*y)*x, or
- x = ((x*y)*z)*x,
use Eq1 as substitution rule for Eq2. If both sides of Eq2 reduce to the same variable, `VERDICT: TRUE`;  else `VERDICT: FALSE`.
RULE: projection family

2.10 Square-reduction family
If Eq1 can be renamed x*x = x, x*(x*x) = x, or (x*x)*x = x, and both sides of Eq2 reduce to the same variable by replacing a repeated x-block by x, `VERDICT: TRUE`; else `VERDICT: FALSE`.
RULE: square reduction

2.11 Missing-variable absorption
If Eq1 can be renamed x*y = T  with T product,
MANDATORY CHECK: Does y appear in T? YES → SKIP this rule. NO → continue
Then Eq1 is x*y = f(x). If Eq2 holds applying LEFT PROJ (p*q=p) recursively, then`VERDICT: TRUE`; else `VERDICT: FALSE`.
MANDATORY CHECK: Does x appear in T? YES → SKIP this rule. NO → continue
Then Eq1 is  x*y = g(y).  If Eq2 holds applying RIGHT PROJ (p*q=q) recursively, then`VERDICT: TRUE`; else `VERDICT: FALSE`.
RULE: missing-variable absorption

STEP 3. CLEAR SYNTACTIC FALSE ROUTES

3.0 Spine Isolation
If Eq1 AND Eq2 are bare and can be renamed Eq1: x = T, Eq2: x = S
MANDATORY CHECK: Does x appear exactly once in T and S? YES → continue. NO → SKIP this rule
Example: Eq1: x = ((x*y)*z)*w → T = ((x*y)*z)*w  AND Eq2: x=(x*y)*z → S = (x*y)*z, then continue
Then trace path from root of T to x: at each * node record L or R and record depth.
- Pure left-spine: all-L 
Example:  x=((x*y)*z)*w → LLL, depth 3
- Pure right-spine: all-R 
Example: x=y*(z*(w*x)) → RRR, depth 3
- Mixed: both L and R.
Do the same for Eq2.
- If Eq1 is pure left-spine, then Eq2 must be pure left-spine with depth >= Eq1. Otherwise `VERDICT: FALSE`.
- If Eq1 is pure right-spine, then Eq2 must be pure right-spine with depth >= Eq1. Otherwise `VERDICT: FALSE`.
- If Eq1 AND Eq2 have same spine type: Eq1 depth=n, Eq2 depth=m, then n must divide m. Otherwise, `VERDICT: FALSE`.
Example: 
- Eq1: x=((x*y)*z)*w → left-spine LLL, depth 3, 
- Eq2 y=(y*z)*w → left-spine LL, depth 2. 
- 3 does not divide 2 → `VERDICT: FALSE`.
RULE: spine isolation

3.1 WORD exclusion
If both sides of Eq1 have the same WORD, but both sides of Eq2 have different WORD, then `VERDICT: FALSE`.
Example: Eq1: (x*y)*z = x*(y*z) → WORD(LHS) = [x,y,z], WORD(RHS) = [x,y,z] → same WORD
Eq2: (x*y)*z = (x*z)*y → WORD(LHS) = [x,y,z], WORD(RHS) = [x,z,y] → different WORD, then `VERDICT: FALSE`
RULE: word exclusion

3.2 PARITY exclusion
FIRST check Eq1: compute PARITY (odd/even count for each variable) of both sides. If parities are equal, rule can fire. Else, SKIP.
Example: Eq1: x*(y*x) = (x*y)*x → PARITY LHS: x=even,y=odd. PARITY RHS: x=even,y=odd. Parities are equal → rule can fire
If Eq1 has equal PARITY on both sides AND Eq2 has different PARITY, then `VERDICT: FALSE`.
RULE: parity exclusion

3.3 Leftmost-variable exclusion
If Eq1 has the same leftmost variable on both sides, but Eq2 does not, then `VERDICT: FALSE`.
RULE: leftmost exclusion

3.4 Rightmost-variable exclusion
If Eq1 has the same rightmost variable on both sides, but Eq2 does not, then `VERDICT: FALSE`.
RULE: rightmost exclusion

3.5 SET exclusion
If both sides of Eq1 use the same SET, but the sides of Eq2 use different SETs, then `VERDICT: FALSE`.
RULE: SET exclusion

3.6 Count exclusion
If both sides of Eq1 have the same exact variable counts, but the sides of Eq2 do not, then `VERDICT: FALSE`
RULE: count exclusion

STEP 4. SMALL FINITE WITNESS ROUTES

4.1 Left-zero
Test magma: a*b = a, then 
- Apply LEFT PROJ (p*q=p) recursively to both sides of Eq1. Do they reduce to the same variable? NO → SKIP this rule. YES → continue.
- Apply LEFT PROJ (p*q=p) recursively to Eq2. Do both sides reduce to different variables? YES → `VERDICT: FALSE`; else → SKIP.
Example: Eq1: (x*y)*z = x*(y*z) → both → x; Eq2: (x*y)*z = y*(x*z) → x ≠ y → `VERDICT: FALSE`
RULE: left-zero 

4.2 Right-zero
Test magma: a*b = b, then
- Apply RIGHT PROJ (p*q=q) recursively to both sides of Eq1. Do they reduce to the same variable? NO → SKIP this rule. YES → continue.
- Apply RIGHT PROJ (p*q=q) recursively to Eq2. Do both sides reduce to different variables? YES → `VERDICT: FALSE`; else → SKIP.
RULE: right-zero

4.3 XOR parity 
Test magma: a*b = a + b mod 2, then
- Compute PARITY of both sides of Eq1. Are they equal? NO → SKIP this rule. YES → continue.
- Compute PARITY of Eq2. Are they equal? YES → SKIP. NO → `VERDICT: FALSE`
Example: Eq1: (x*y)*z = x*(y*z) → same parity; Eq2: x*(y*y) = x → parity differs → `VERDICT: FALSE`
RULE: XOR parity 

4.4 Constant magma
Test magma: a*b = k, with k constant. Then
- Evaluate Eq1 by replacing every product using CONST (p*q=k). Do both sides reduce to the same value? NO → SKIP this rule. YES → continue.
- Evaluate Eq2 by replacing every product using CONST (p*q=k). Do sides reduce to different values? YES → `VERDICT: FALSE`; else → SKIP.
Example: Eq1: x*y = z*w → both → k; Eq2: x = y*z → x ≠ k → `VERDICT: FALSE`
RULE: constant magma

DEFAULT: No route in STEPs 2-4 fired, then `VERDICT: FALSE`. REASONING must say "default false fallback".