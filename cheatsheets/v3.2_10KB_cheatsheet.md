You are solving equational implication problems over magmas: one binary operation * and no axioms except Equation 1.
Task: does Equation 1 imply Equation 2 over ALL magmas?

Equation 1:
{{ equation1 }}

Equation 2:
{{ equation2 }}

OUTPUT FORMAT (mandatory)
Your FIRST line must be exactly one of:
VERDICT: TRUE
VERDICT: FALSE
Then optionally one short line:
RULE: <very short reason>
No other format.

IMPORTANT
- Follow the STEP protocol exactly.
- Use the FIRST definitive rule that fires.
- Never override a fired rule with free-form algebra.
- A TRUE verdict must come from an exact family solver, a listed theorem, or an exact tiny rewrite.
- A FALSE verdict must come from an exact witness route below.
- For FALSE, do not improvise a magma. Use only the listed witnesses.
- A witness is valid only if Equation 1 is obviously true there for ALL assignments by the stated invariant; otherwise reject that witness.
- Failure to find a FALSE witness is NOT evidence for TRUE.
- Keep reasoning internal; keep output minimal.

STEP 0. META-PROCESS
READ: parse both equations as binary trees.
ANALYZE: compute the data in STEP 1.
PLAN: choose one route only: exact-TRUE / exact-FALSE / tiny rewrite.
IMPLEMENT: execute only that route.
VERIFY internally: what exactly am I doing, why does it decide the case, and did I use only listed rules?
If not clear, do not trust the route.

STEP 1. SAFE NORMALIZATION
For classification only, you may:
- alpha-rename variables by first appearance;
- swap the two sides of an equation;
- dualize BOTH equations simultaneously by reversing every product.
Never dualize only one equation.

For each side record:
- side type: bare variable or genuine product;
- leftmost variable;
- rightmost variable;
- leaf word = variables read left-to-right, ignoring parentheses;
- parity vector = variable counts mod 2;
- left spine depth LSD: LSD(x)=0, LSD(a*b)=1+LSD(a);
- right spine depth RSD: RSD(x)=0, RSD(a*b)=1+RSD(b).
For commutativity only, use CN: CN(x)=x; CN(a*b)=sort(CN(a),CN(b)) with tree shape preserved.

STEP 2. TRIVIAL DECISIONS
2.1 If Equation 2 is a tautology t=t, answer TRUE.
2.2 If Equation 1 and Equation 2 are identical up to alpha-renaming, side-swap, and optional simultaneous dualization, answer TRUE.
2.3 If Equation 1 is a tautology and Equation 2 is not, answer FALSE.

STEP 3. EXACT TRUE SOURCE FAMILIES
If Equation 1 matches one of these exactly, solve and STOP.

3.1 Singleton / collapse.
If one side is a bare variable v and the other side contains no v anywhere, then Equation 1 forces the singleton law. Examples: x=y, x=y*y, x=y*z, x=(y*z)*w. Then every Equation 2 is TRUE.

3.2 Left projection.
If Equation 1 is x=x*y or x*y=x, then a*b=a. Every term equals its leftmost variable. Equation 2 is TRUE iff its two sides have the same leftmost variable; else FALSE.

3.3 Right projection.
If Equation 1 is x=y*x or x*y=y, then a*b=b. Every term equals its rightmost variable. Equation 2 is TRUE iff its two sides have the same rightmost variable; else FALSE.

3.4 Right-ignored-input family.
If Equation 1 is x*x=x*y or x*y=x*z, then x*u=x*v for all u,v. Terms are determined by leftmost variable plus left-spine shape. Equation 2 is TRUE iff its two sides have the same leftmost variable and the same LSD; else FALSE.

3.5 Left-ignored-input family.
If Equation 1 is x*x=y*x or x*y=z*y, then u*x=v*x for all u,v. Terms are determined by rightmost variable plus right-spine shape. Equation 2 is TRUE iff its two sides have the same rightmost variable and the same RSD; else FALSE.

3.6 Constant-operation family.
If Equation 1 is x*y=z*w or x*x=y*z, then every genuine product term has one constant value c. Equation 2 is TRUE iff both sides of Equation 2 are genuine product terms, or Equation 2 is tautological; else FALSE.

3.7 Associativity.
If Equation 1 is x*(y*z)=(x*y)*z or reverse, then Equation 2 is TRUE iff both sides have the same leaf word; else FALSE.

3.8 Commutativity.
If Equation 1 is x*y=y*x, then Equation 2 is TRUE iff both sides have the same commutative normal form CN; else FALSE.

3.9 Idempotence.
If Equation 1 is x*x=x or x=x*x, use only local contractions/expansions u*u <-> u. If both sides of Equation 2 reduce to the same tree, answer TRUE. Otherwise continue.

3.10 Finite involution-on-right family.
Use this only for the exact sources x=y*(x*x) and x=y*((z*x)*x).
On finite magmas these sources force the operation to depend only on the right input via an involution, so one multiplication toggles the rightmost variable once. Therefore Equation 2 is TRUE iff its two sides have the same rightmost variable and the same RSD parity (RSD mod 2); else continue.

3.11 Product-left projection lemma.
If Equation 1 is x=(y*z)*x, you may use the exact rewrite (a*b)*c -> c anywhere, because it is just Equation 1 with renamed variables. Apply this greedily to Equation 2. If both sides become identical, answer TRUE.

3.12 Dev-slice exact theorem.
If Equation 1 is x=(y*z)*x and Equation 2 is x*y = y*((y*x)*y), answer TRUE.

STEP 4. EXACT FALSE WITNESS ROUTES
Use only these. A route is allowed only if Equation 1 is obviously valid there by the named invariant.

4.1 Left-zero witness a*b=a.
Use only when Equation 1 reduces to the same leftmost variable on both sides. Then if Equation 2 reduces to different leftmost variables, answer FALSE.

4.2 Right-zero witness a*b=b.
Use only when Equation 1 reduces to the same rightmost variable on both sides. Then if Equation 2 reduces to different rightmost variables, answer FALSE.

4.3 Word witness.
Interpret * as nonassociative word-building. Use only when the two sides of Equation 1 have exactly the same leaf word. If Equation 2 has different leaf words, answer FALSE.

4.4 Parity witness.
Interpret * as addition on (F2)^V. Use only when the two sides of Equation 1 have the same parity vector. If Equation 2 has different parity vectors, answer FALSE.

4.5 Constant-0 witness.
Interpret every genuine product term as 0, while a bare variable stays free. If Equation 1 is obviously valid there for all assignments but Equation 2 has one bare-variable side and one genuine-product side, answer FALSE.

4.6 Witness rejection rule.
If a proposed witness makes Equation 1 become x=y, x=0, or any other non-identity condition that clearly fails for some assignment, then that witness is INVALID and must be discarded.

STEP 5. NAMED HIGH-LEVERAGE THEOREMS
If Equation 1 matches one of these exactly, use the stated consequence and STOP.

5.1 x = y*((z*x)*(z*z))  => singleton law => TRUE.
5.2 x = (y*x)*((x*z)*z) => singleton law => TRUE.
5.3 x*y = (y*y)*x       => commutativity. Then solve by CN.
5.4 x = (y*z)*(y*(x*z)) => parity law. Equation 2 is TRUE iff both sides have the same parity vector; else FALSE.
5.5 x = y*(x*y) and x = (y*x)*y are equivalent. If Equation 1 is one and Equation 2 is the other, answer TRUE.

STEP 6. TINY EXACT REWRITE FALLBACK
Use only if STEPs 3-5 did not decide the case.
Allowed move: if Equation 1 is L=R, then under any substitution and one-hole context you may replace L by R or R by L.
Restrictions:
- at most 2 rewrite steps;
- exact subterm replacement only;
- no invented lemmas except the explicit one in 3.11;
- mainly for commutativity, associativity, idempotence, projection, or ignored-input families.
If 1-2 exact rewrites convert one side of Equation 2 into the other, answer TRUE.

STEP 7. FINAL DEFAULT
If you are here, no exact TRUE rule fired and no exact FALSE witness fired.
Answer FALSE.

Now solve the given pair.
