You are solving equational implication problems over magmas: one binary operation * and no axioms except Equation 1.
Task: does Equation 1 imply Equation 2 over ALL magmas?

OUTPUT FORMAT (mandatory)
Your FIRST line must be exactly one of:
VERDICT: TRUE
VERDICT: FALSE
Then optionally one short line:
RULE: <very short reason>
No other format.

IMPORTANT
- Follow the STEP protocol exactly.
- Stop at the FIRST definitive rule.
- Never override a fired rule with free-form algebra.
- Prefer exact TRUE routes. Use FALSE only from listed syntactic exclusions.
- Do NOT improvise magma tables or counterexamples.
- If a FALSE route is not literally justified by its invariant, reject it.
- Failure to prove FALSE is not evidence for TRUE; failure to prove TRUE is not evidence for FALSE beyond STEP 6.
- Keep reasoning internal; keep output minimal.

STEP 0. META-PROCESS
READ the two equations as binary trees.
ANALYZE only the data in STEP 1.
PLAN one route only: exact-family / exact-theorem / syntactic-FALSE / tiny rewrite.
IMPLEMENT that single route.
VERIFY internally: what am I doing, why does it decide the case, and did I use only listed rules?
If not clear, reject the route.

STEP 1. SAFE NORMALIZATION
For classification only, you may:
- alpha-rename variables by first appearance;
- swap the two sides of an equation;
- dualize BOTH equations simultaneously by reversing every product.
Never dualize only one equation.

Record for each side:
- side type: bare variable or genuine product;
- leftmost variable LM;
- rightmost variable RM;
- leaf word LW = variables read left-to-right, ignoring parentheses;
- parity vector PV = variable counts mod 2;
- left spine depth LSD: LSD(x)=0, LSD(a*b)=1+LSD(a);
- right spine depth RSD: RSD(x)=0, RSD(a*b)=1+RSD(b).
For commutativity only use CN: CN(x)=x; CN(a*b)=sort(CN(a),CN(b)) with tree shape preserved.

STEP 2. TRIVIAL DECISIONS
2.1 If Equation 2 is a tautology t=t, answer TRUE.
2.2 If Equation 1 and Equation 2 are identical up to alpha-renaming, side-swap, and optional simultaneous dualization, answer TRUE.
2.3 If Equation 1 is a tautology and Equation 2 is not, answer FALSE.

STEP 3. EXACT TRUE SOURCE FAMILIES
If Equation 1 matches one of these exactly, solve and STOP.

3.1 Singleton / collapse.
If one side is a bare variable v and the other side contains no v anywhere, Equation 1 forces the singleton law. Examples: x=y, x=y*y, x=y*z, x=(y*z)*w. Then every Equation 2 is TRUE.

3.2 Left projection.
If Equation 1 is x=x*y or x*y=x, then a*b=a. Every term equals its leftmost variable. Equation 2 is TRUE iff its two sides have the same LM; else FALSE.

3.3 Right projection.
If Equation 1 is x=y*x or x*y=y, then a*b=b. Every term equals its rightmost variable. Equation 2 is TRUE iff its two sides have the same RM; else FALSE.

3.4 Right-ignored-input family.
If Equation 1 is x*x=x*y or x*y=x*z, then x*u=x*v for all u,v. Terms are determined by LM and LSD. Equation 2 is TRUE iff its two sides have the same LM and the same LSD; else FALSE.

3.5 Left-ignored-input family.
If Equation 1 is x*x=y*x or x*y=z*y, then u*x=v*x for all u,v. Terms are determined by RM and RSD. Equation 2 is TRUE iff its two sides have the same RM and the same RSD; else FALSE.

3.6 Constant-operation family.
If Equation 1 is x*y=z*w or x*x=y*z, then every genuine product term has one constant value c. Equation 2 is TRUE iff both sides of Equation 2 are genuine product terms, or Equation 2 is tautological; else FALSE.

3.7 Associativity.
If Equation 1 is x*(y*z)=(x*y)*z or reverse, Equation 2 is TRUE iff its two sides have the same LW; else FALSE.

3.8 Commutativity.
If Equation 1 is x*y=y*x, Equation 2 is TRUE iff its two sides have the same CN; else FALSE.

3.9 Idempotence.
If Equation 1 is x*x=x or x=x*x, use only local contractions/expansions u*u <-> u. If both sides of Equation 2 reduce to the same tree, answer TRUE. Otherwise continue.

STEP 4. HIGH-YIELD EXACT THEOREMS
If Equation 1 matches one of these exactly, use the stated consequence and STOP.

4.1 Product-right projection.
If Equation 1 is x=(y*z)*x, then under any substitution you may rewrite (a*b)*c -> c. Apply greedily to Equation 2. If both sides become identical, answer TRUE.

4.2 Product-left projection.
If Equation 1 is x=x*(y*z), then under any substitution you may rewrite c*(a*b) -> c. Apply greedily to Equation 2. If both sides become identical, answer TRUE.

4.3 Back-absorption.
If Equation 1 is x=x*((y*z)*x), then it is equivalent to x*(y*x)=x. Use local contraction a*(b*a) -> a on Equation 2. If both sides become identical, answer TRUE.

4.4 Square-left projection family.
If Equation 1 is x=x*(y*((y*y)*z)) or x=x*(y*((y*z)*w)) or x=x*(y*((z*z)*w)), then Equation 1 is equivalent to left projection a*b=a. Solve Equation 2 exactly by LM as in STEP 3.2.

4.5 Square-prefix family.
If Equation 1 is x=(x*x)*y or x=(x*x)*(y*z), then under any substitution you may rewrite (a*a)*b -> a. Apply greedily to Equation 2. If both sides become identical, answer TRUE.

4.6 Singleton boosters.
If Equation 1 is x=y*((z*x)*(z*z)) or x=(y*x)*((x*z)*z), then Equation 1 forces the singleton law. Answer TRUE.

4.7 Commutativity booster.
If Equation 1 is x*y=(y*y)*x, then Equation 1 implies commutativity. Solve Equation 2 by CN as in STEP 3.8.

4.8 Parity booster.
If Equation 1 is x=(y*z)*(y*(x*z)), then Equation 2 is TRUE iff its two sides have the same PV; else FALSE.

4.9 Putnam pair.
If Equation 1 is x=y*(x*y) and Equation 2 is x=(y*x)*y, answer TRUE.
If Equation 1 is x=(y*x)*y and Equation 2 is x=y*(x*y), answer TRUE.

STEP 5. SAFE SYNTACTIC FALSE ROUTES
Use FALSE only from these exact invariants.

5.1 Word exclusion.
If the two sides of Equation 1 have exactly the same LW, but the two sides of Equation 2 have different LW, answer FALSE.

5.2 Parity exclusion.
If the two sides of Equation 1 have exactly the same PV, but the two sides of Equation 2 have different PV, answer FALSE.

Do NOT use left-zero, right-zero, constant-0, or any improvised witness.

STEP 6. TINY EXACT REWRITE FALLBACK
Use only if STEPs 3-5 did not decide the case.
Allowed move: if Equation 1 is L=R, then under any substitution and one-hole context you may replace L by R or R by L.
Restrictions:
- at most 2 rewrite steps;
- exact subterm replacement only;
- no invented lemmas except those explicitly listed in STEP 4;
- use this mainly for associativity, commutativity, idempotence, projection, or ignored-input families.
If 1-2 exact rewrites convert one side of Equation 2 into the other, answer TRUE.

STEP 7. FINAL DEFAULT
If no exact TRUE route fired and no syntactic FALSE route fired, answer FALSE.

Now solve this pair.

Equation 1:
{{ equation1 }}

Equation 2:
{{ equation2 }}
